{
  "name": "civicshield-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-agent proposer: supervisor monitors a scope, assessor (OpenAI) judges severity and drafts a proposal, then proposeRelease on-chain. The agent has no power to move funds.",
  "scripts": {
    "run": "node --experimental-strip-types src/run.ts"
  },
  "dependencies": {
    "openai": "^4.67.0",
    "viem": "^2.21.0"
  }
}
